package com.zipcodewilmington.froilansfarm;

public class Test {

    // setting the plot
    // Aircraft Vehicle ()
}// Aircraft aircraft = new CroDuster()
;
//Assert.assertTrue(aircraft isntance of Vehicle);


//AircraftFLy();
// Aircraft aircraft = new Cropduster();
//Assert.assertTrue(aircraft.fly());


//CropDusterisFarmVehicle
//CropDuster cropDuster = new CropDuster();
//Assert.assertTrue(cropDuster instanceOf FarmVehicle);


//CropDusterIsAircraft
//CropDuster cropDuster = new Cropduster()
//assert.assertTrue(CropDuster isntance of Aircraft


// CropDusterFertilizeCropRows
// CropDuser corpDuster = ne CropDuster
// CropRow cropCrow = new CropRow();
//j Crop crop = new TomatoePlant();
// cropRow.add(crop)
// cropDuster.fertlize(cropRow);
//Assert.assertTrue(crop.hasBeenFertlized());



//ProduceYieldUnfertilized()
//Produce produce = new TomatoPlant()
//Assert.assertTrue(produce.yield() == null)

// produceyieldfertilized
////Produce produce = new TomatoPlant()
//produce.setHasBeenFertilized(true)
////Assert.assertTrue(produce.yield() instajnceof Edible)

//CropisProduce
//Assert.assertTrue(crop instanceof Produce);
//Crop

//create an interface aircraft
//create class corpduster implements aircraft
//create method fly in air craft
// create method fertilize in cropduster
//implement method fly in cropduster
//hasBeenFertilized method in corp
//make tomatoe plant implement produce tomatoeplant extend Crop<tomto> implenets produce<tomato>
// mak crop implement proudce<T>
// create a method setHasbeenFetilized in produced
//create method sethasbeenHarvvested on crop
// Cornstalk create a class conrstank .... cornstalk extend Crop<EarCorn>
//create class person implenets noisemaker ..then implenet methods for that
//make person implement eater
// go to farmer extends Person implenets Rider, Botanist then insert super
// Create class sheleter <T> extends ArrayList<T>
// FroilanFarmer compile create class Froilan
//Make Froilan extend Farmer then create a construtor matching super, Froilan then needs a constructor generate that and it calls the super "Froilan"
//getFarm is a method we create for Farmer make it return null
//getFarmField create a method in farm field return null
// create class CornRow extend CropRow<CornStalk>
//create class TomatoeRow extenmds Croprow <Tomato plant>
// create method getChickenCount int in farm
// getHorse stable method List<HorseStable>
//getHourseCount int return 0
//Aircraft get aircraft
//Vehicles getVehicles LIst<vehicles>
//froilanad extends Farmer impletents PIlot
// create interface Pilot
// create a construct super Froilanda
//CropDusteer needs implenet Ridable to getRider
//farmer super(name)
//making the Monday compile cast argumenet to Ridable, make dismojnt return  boolean
//change to FroilandaRideHorse thats for each day riding eachhourse
// copy past change Friolan Friolana Ride to Friolan Friolana feedhorse this will be rach day
//cAnimal<T extends Edible> implenets Eater <T>
//that stuff on monday goes everyother day